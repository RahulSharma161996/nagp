apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: nagp-2026
type: Opaque
data:
  # Base64-encoded password goes here (do not commit the real value).
  # Example: echo -n "YourStrongPassword" | base64
  DB_PASSWORD: REPLACE_ME_BASE64
